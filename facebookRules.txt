! === 1. REELS & SUGGESTED (STABLE VERSION) ===
! Only targets the specific article, not the whole feed container
facebook.com##div[role="feed"] [role="article"]:has-text(/Suggested for you/):style(display:none !important;)
facebook.com##div[role="feed"] [role="article"]:has-text(/Reels and short videos/):style(display:none !important;)
facebook.com##div[aria-label="Reels"]
facebook.com##div[aria-label="Stories"]

! === 2. SPONSORED CONTENT (RELIABLE) ===
! Targets the specific 'Sponsored' text to avoid accidental feed breakage
facebook.com##div[role="feed"] [role="article"]:has(span:has-text(/^Sponsored$/)):style(display:none !important;)

! === 3. THE SCRIPTLET (KEEP DISABLED IF FEED STICKS) ===
! Remove the '!' below ONLY if you have "I am an advanced user" checked in uBO
! facebook.com##+js(facebook-feeds-filter, engagement|suggested|promotion)

! === 4. FEED REPAIR (THE FIX) ===
! Removed the "div:empty" rule because it breaks infinite scroll. 
! Instead, we ensure the feed container itself stays visible.
facebook.com##div[role="feed"]:style(display: block !important; opacity: 1 !important;)
